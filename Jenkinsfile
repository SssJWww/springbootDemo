#!/bin/groovy

pipeline {
    //agent any
    agent {
        label any
    }

    stages {
        stage('Initialization') {
            steps {
                echo 'init success'
            }
        }

        stage('Build') {
            steps {
                echo 'build success'
            }
        }
    }
}