#!/bin/groovy

pipeline {
    agent any
//     agent {
//         label any
//     }

    stages {
        stage('Initialization') {
            steps {
                script {
                    echo 'init success'
                }
            }
        }

        stage('Build') {
            steps {
                script {
                    echo 'build success'
                }
            }
        }
    }
}